export class Point {
  x: number;
  y: number;
  offsetX: number;
  offsetY: number;

  constructor(x: number, y: number, offsetX: number, offsetY: number) {
    this.x = x;
    this.y = y;
    this.offsetX = offsetX;
    this.offsetY = offsetY;
  }
}
